# GitHub Pages configuration
title: ihandai — Go AI Framework
description: Provider-agnostic Go library for RAG, agents, workflows, and production AI — swap providers without rewriting code.
remote_theme: pages-themes/cayman@v0.2.0
plugins:
  - jekyll-remote-theme
  - jekyll-seo-tag
  - jekyll-relative-links
relative_links:
  enabled: true
  collections: true
show_downloads: true
